'use strict';

var blacklist = ['freelist', 'sys'];

module.exports = Object.keys(process.binding('natives')).filter(function (x) {
	return !/^_|^internal|\//.test(x) && blacklist.indexOf(x) === -1;
}).sort();