'use strict';

var _chai = require('chai');

var _getPathVar = require('./get-path-var');

var _getPathVar2 = _interopRequireDefault(_getPathVar);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

describe('get-path-var', function () {
  it('should provide me with PATH when the platform is not `win32`', function () {
    var env = { PATH: '/usr/bin' };
    var platform = 'darwin';
    (0, _chai.expect)((0, _getPathVar2.default)(env, platform)).to.equal('PATH');
  });

  describe('`win32` platform', function () {
    it('should provide me with `Path` if env does not contain `PATH`', function () {
      var env = { Path: '/usr/bin' };
      var platform = 'win32';
      (0, _chai.expect)((0, _getPathVar2.default)(env, platform)).to.equal('Path');
    });

    it('should provide me with `PATH` when env contains `PATH`', function () {
      var env = { otherThing: '123', PATH: '/usr/bin' };
      var platform = 'win32';
      (0, _chai.expect)((0, _getPathVar2.default)(env, platform)).to.equal('PATH');
    });
  });
});