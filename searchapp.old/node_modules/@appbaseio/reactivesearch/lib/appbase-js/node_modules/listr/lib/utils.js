'use strict';

var isStream = require('is-stream');

// RxJS@6 symbol (https://github.com/sindresorhus/is-observable/issues/1#issuecomment-387843191)
var symbolObservable = typeof Symbol === 'function' && Symbol.observable || '@@observable'; // eslint-disable-line no-mixed-operators

var isObservable = function isObservable(obj) {
  return Boolean(obj && obj[symbolObservable] && obj === obj[symbolObservable]()) || require('is-observable')(obj);
};

exports.isListr = function (obj) {
  return Boolean(obj && obj.setRenderer && obj.add && obj.run);
};
exports.isObservable = function (obj) {
  return isObservable(obj);
};
exports.isStream = function (obj) {
  return isStream(obj) && !isObservable(obj);
};