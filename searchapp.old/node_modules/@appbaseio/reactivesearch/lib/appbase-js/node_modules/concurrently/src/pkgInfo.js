'use strict';

var readPkg = require('read-pkg');

var pkg = void 0;

function getPkg() {
    if (!pkg) {
        pkg = readPkg.sync();
    }
    return pkg;
}

exports.getScripts = function () {
    var pkg = getPkg();
    return Object.keys(pkg.scripts || {});
};